{
	"name": "cloudflare-workers",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"cf-typegen": "wrangler types",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "NODE_DEBUG=vitest-pool-workers:module-fallback,vitest-pool-workers:index vitest --run"
	},
	"dependencies": {
		"@graphql-ice/engine": "0.0.8"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.5.2",
		"@cloudflare/workers-types": "^4.20241218.0",
		"wrangler": "^3.99.0",
		"typescript": "^5.5.2"
	}
}
